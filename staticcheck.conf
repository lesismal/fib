# U1000: helpers are split across per-OS files, so code used on one GOOS
# is reported unused on another.
# SA1019: the Windows engine uses syscall IOCP APIs on purpose to keep the
# module free of golang.org/x/sys.
checks = ["inherit", "-U1000", "-SA1019"]
